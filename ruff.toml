target-version = "py312"

[lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "BLE"]
ignore = [
    # Broad exception catches are deliberate in this integration: push/fetch/
    # sync failures must degrade to logging, never crash the HA event loop.
    "BLE001",
]
